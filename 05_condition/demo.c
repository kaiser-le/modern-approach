//build condition by logic expression(boolean)
//logic operator may affect the side effect
#include <stdio.h>
#include <stdbool.h>

int main(void)
{
	bool flag = true;
	printf("flag = %u\n", flag);

	return 0;
}