#include <stdio.h>
#include <ctype.h>

int main(void)
{
	// char ch;
	// ch = getchar();
	// // if (ch >= 'a' && ch <= 'z')
	// // 	ch = ch - 'a' + 'A';
	// ch = toupper(ch);
	// printf("ch = %c\n", ch);

	printf("Size of long long int: %zu\n", sizeof(long long));

	return 0;
}